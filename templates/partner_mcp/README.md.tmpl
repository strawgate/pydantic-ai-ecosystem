# Pydantic AI {{ display_name }}

Implementation package for {{ display_name }} capability integrations.

Initial direction:

- `{{ capability_class_name }}` as the primary surface
- MCP-backed defaults with partner-specific instructions
- a clean path to deeper workflow logic later

This package should become the default {{ display_name }} partner capability for Pydantic AI agents.

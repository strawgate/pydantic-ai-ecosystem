# Pydantic AI Partner MCP

Shared foundation for partner capability packages that are primarily:

- MCP connectivity
- auth and header defaults
- prompts and instructions
- basic write-safety policy

This package exists to make "MCP plus a little opinionation" cheap enough to scale.

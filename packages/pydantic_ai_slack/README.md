# Pydantic AI Slack

Implementation package for Slack capability integrations.

Initial direction:

- `SlackCapability` as the primary surface
- channel- and thread-aware instructions
- explicit defaults around when posting is allowed

This package should become the default collaboration capability for Slack-based agents.

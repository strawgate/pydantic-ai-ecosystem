# Pydantic AI Notion

Implementation package for Notion capability integrations.

Initial direction:

- `NotionCapability` as the primary surface
- MCP-backed defaults with partner-specific instructions
- a clean path to deeper workflow logic later

This package should become the default Notion partner capability for Pydantic AI agents.
